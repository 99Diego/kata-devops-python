name: CI/CD Pipeline - Python DevOps Kata

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  # -------------------------
  # LINT
  # -------------------------
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install flake8
        run: pip install flake8

      - name: Run flake8
        run: flake8 app tests

  # -------------------------
  # TESTS
  # -------------------------
  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: pytest

  # -------------------------
  # SECURITY SCAN (DEPENDENCIES)
  # -------------------------
  security-code:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pip-audit
        run: pip install pip-audit

      - name: Run dependency scan
        run: pip-audit

  # -------------------------
  # SEMANTIC VERSIONING
  # -------------------------
  release:
    runs-on: ubuntu-latest
    needs: security-code
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install semantic-release and plugins
        run: |
          npm install -g semantic-release \
            @semantic-release/commit-analyzer \
            @semantic-release/release-notes-generator \
            @semantic-release/changelog \
            @semantic-release/github

      - name: Run semantic-release
        run: semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  

  # -------------------------
  # BUILD & PUSH DOCKER IMAGE
  # -------------------------
  docker:
    runs-on: ubuntu-latest
    needs: release
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          push: true
          tags: |
            99diego/kata-devops-python:${{ env.VERSION }}
            99diego/kata-devops-python:latest

  # -------------------------
  # SECURITY SCAN (IMAGE)
  # -------------------------
  security-image:
    runs-on: ubuntu-latest
    needs: docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: 99diego/kata-devops-python:${{ env.VERSION }}
          exit-code: 1
          severity: CRITICAL,HIGH

  # -------------------------
  # DEPLOY (DESACTIVADO)
  # -------------------------
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: security-image
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Deploy via SSH
  #       run: |
  #         echo "Deploy stage ready when infra exists"

